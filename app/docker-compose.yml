services:
  frontend:
    ports: 
      - "80:80"
    build: ./frontend
    networks:
      - main_network
  backend:
    expose:
     - "8000"
    build: ./backend
    networks:
     - main_network
networks:
  main_network:
    driver: bridge
